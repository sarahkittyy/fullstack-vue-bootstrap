version: "3"
services:
  frontend:
    container_name: frontend
    build: .
    ports:
     - "8000:8000"
    volumes:
     - ./:/workspace
     - /workspace/node_modules
    entrypoint: ["npm", "run", "serve"]